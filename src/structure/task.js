/**
 * tasks in program
 * 
 * Created By SH
 */

module.exports = {
  initTemplate: function(template, projectName) {
    console.log(template, projectName);
    log.info('success')
  },
}